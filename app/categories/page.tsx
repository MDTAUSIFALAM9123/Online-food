import Link from 'next/link';
import { categories } from '@/lib/data';
export default function CategoriesPage() { return <div className="mx-auto max-w-7xl px-4 py-6 lg:px-8"><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Browse everything</p><h1 className="mt-2 font-display text-3xl font-bold">Food categories</h1><div className="mt-7 grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-7">{categories.map((category) => <Link href={`/restaurants?category=${category.id}`} key={category.id} className="group rounded-2xl border border-border/60 bg-card p-3 text-center shadow-sm hover:-translate-y-1 hover:shadow-lg"><div className="aspect-square overflow-hidden rounded-xl"><img src={category.image} alt={category.name} className="h-full w-full object-cover transition-transform group-hover:scale-110" /></div><p className="mt-3 text-sm font-semibold">{category.name}</p></Link>)}</div></div>; }
