'use client';

import { Bell, Globe2, LockKeyhole, Moon, Save, ShieldCheck } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Switch } from '@/components/ui/switch';
import { toast } from 'sonner';
export default function SettingsPage() { return <div className="mx-auto max-w-3xl px-4 py-6 lg:px-8"><h1 className="font-display text-3xl font-bold">Settings</h1><p className="mt-1 text-muted-foreground">Personalize your FoodGo experience.</p><div className="mt-6 space-y-4"><div className="rounded-2xl border border-border/60 bg-card p-5"><h2 className="font-display font-semibold">Preferences</h2>{[['Order updates','Get real-time updates about your orders.',Bell],['Marketing notifications','Receive new offers and restaurant recommendations.',ShieldCheck],['Dark mode','Use darker colors in low-light environments.',Moon]].map(([label,description,Icon]) => { const Component = Icon as React.ComponentType<{ className?: string }>; return <div key={String(label)} className="mt-5 flex items-center gap-3"><Component className="h-5 w-5 text-primary" /><div className="flex-1"><p className="text-sm font-medium">{String(label)}</p><p className="mt-1 text-xs text-muted-foreground">{String(description)}</p></div><Switch defaultChecked={label !== 'Dark mode'} /></div>; })}</div><div className="rounded-2xl border border-border/60 bg-card p-5"><h2 className="font-display font-semibold">Privacy & security</h2><div className="mt-5 flex items-center gap-3"><LockKeyhole className="h-5 w-5 text-primary" /><div className="flex-1"><p className="text-sm font-medium">Change password</p><p className="mt-1 text-xs text-muted-foreground">Keep your account protected.</p></div><Button variant="outline">Update</Button></div><div className="mt-5 flex items-center gap-3"><Globe2 className="h-5 w-5 text-primary" /><div className="flex-1"><p className="text-sm font-medium">Language</p><p className="mt-1 text-xs text-muted-foreground">English (India)</p></div><Button variant="outline">Change</Button></div></div></div><Button className="mt-5" onClick={() => toast.success('Settings saved')}><Save className="mr-2 h-4 w-4" /> Save changes</Button></div>; }
