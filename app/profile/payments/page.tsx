'use client';

import { useState } from 'react';
import { Check, CreditCard, Plus, Smartphone, Trash2, WalletCards } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { toast } from 'sonner';
const methods = [{ id: 'upi', name: 'Arjun Mehta', detail: 'arjun.mehta@oksbi', icon: Smartphone }, { id: 'card', name: 'HDFC Bank Credit Card', detail: '•••• 4821', icon: CreditCard }, { id: 'wallet', name: 'FoodGo Wallet', detail: 'Balance ₹250', icon: WalletCards }];
export default function PaymentsPage() { const [selected, setSelected] = useState('upi'); return <div className="mx-auto max-w-3xl px-4 py-6 lg:px-8"><div className="flex items-center justify-between"><div><h1 className="font-display text-3xl font-bold">Payment methods</h1><p className="mt-1 text-muted-foreground">Your payment details stay private and secure.</p></div><Button onClick={() => toast.success('Payment method form opened')}><Plus className="mr-2 h-4 w-4" /> Add method</Button></div><div className="mt-6 space-y-3">{methods.map(({ id, name, detail, icon: Icon }) => <div key={id} className="flex items-center gap-3 rounded-2xl border border-border/60 bg-card p-5"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-primary/10 text-primary"><Icon className="h-5 w-5" /></div><div className="flex-1"><p className="font-semibold">{name}</p><p className="mt-1 text-sm text-muted-foreground">{detail}</p></div><button onClick={() => setSelected(id)} className={`flex h-6 w-6 items-center justify-center rounded-full border-2 ${selected === id ? 'border-primary bg-primary text-white' : 'border-border'}`}>{selected === id && <Check className="h-3 w-3" />}</button><Button variant="ghost" size="icon" onClick={() => toast.success('Payment method removed')}><Trash2 className="h-4 w-4 text-destructive" /></Button></div>)}</div></div>; }
