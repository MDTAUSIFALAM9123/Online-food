'use client';

import { useState } from 'react';
import { Home, MapPin, Pencil, Plus, Trash2, Briefcase } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { addresses } from '@/lib/data';
import { toast } from 'sonner';
export default function AddressesPage() { const [saved, setSaved] = useState(addresses); return <div className="mx-auto max-w-3xl px-4 py-6 lg:px-8"><div className="flex items-center justify-between"><div><h1 className="font-display text-3xl font-bold">Saved addresses</h1><p className="mt-1 text-muted-foreground">Manage where FoodGo should deliver.</p></div><Button onClick={() => toast.success('Address form opened')}><Plus className="mr-2 h-4 w-4" /> Add New</Button></div><div className="mt-6 space-y-4">{saved.map((address) => <div key={address.id} className="rounded-2xl border border-border/60 bg-card p-5"><div className="flex items-start gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary/10 text-primary">{address.label === 'Work' ? <Briefcase className="h-5 w-5" /> : <Home className="h-5 w-5" />}</div><div className="flex-1"><div className="flex items-center gap-2"><h2 className="font-display font-semibold">{address.label}</h2>{address.isDefault && <span className="rounded-full bg-success/10 px-2 py-0.5 text-[10px] font-bold text-success">DEFAULT</span>}</div><p className="mt-2 text-sm text-muted-foreground">{address.address}, {address.landmark}, {address.city}, {address.state} - {address.pincode}</p><p className="mt-1 text-sm text-muted-foreground">{address.name} • {address.phone}</p></div><div className="flex gap-1"><Button variant="ghost" size="icon" onClick={() => toast.success('Address editor opened')}><Pencil className="h-4 w-4" /></Button><Button variant="ghost" size="icon" onClick={() => setSaved((current) => current.filter((item) => item.id !== address.id))}><Trash2 className="h-4 w-4 text-destructive" /></Button></div></div></div>)}</div></div>; }
