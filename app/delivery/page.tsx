'use client';

import { useState } from 'react';
import { Banknote, Bike, CheckCircle2, Clock3, MapPin, Package, Phone, Star, UserRound } from 'lucide-react';
import { PanelShell, MetricCard, type PanelNavItem } from '@/components/panels/panel-shell';
import { Button } from '@/components/ui/button';
import { Switch } from '@/components/ui/switch';
import { toast } from 'sonner';
const nav: PanelNavItem[] = [{ href: '/delivery', label: 'Dashboard', icon: Bike }, { href: '/delivery/orders', label: 'Delivery Requests', icon: Package }, { href: '/delivery/active', label: 'Active Delivery', icon: MapPin }, { href: '/delivery/history', label: 'History', icon: CheckCircle2 }, { href: '/delivery/earnings', label: 'Earnings', icon: Banknote }, { href: '/delivery/profile', label: 'My Profile', icon: UserRound }];
export default function DeliveryPage() { const [online, setOnline] = useState(true); return <PanelShell title="Delivery dashboard" subtitle="Stay on top of your deliveries and earnings." brand="FoodGo Delivery" navItems={nav} accent="bg-success"><div className="flex flex-wrap items-center justify-between gap-4 rounded-2xl border border-border/60 bg-card p-5"><div><p className="text-sm text-muted-foreground">Your availability</p><div className="mt-2 flex items-center gap-3"><span className={`h-3 w-3 rounded-full ${online ? 'bg-success' : 'bg-muted-foreground'}`} /><span className="font-display text-xl font-bold">{online ? 'You are online' : 'You are offline'}</span></div></div><Switch checked={online} onCheckedChange={setOnline} /></div><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Today&apos;s Deliveries" value="18" change="+4 from yesterday" icon={Bike} /><MetricCard label="Today&apos;s Earnings" value="₹1,280" change="+₹180 bonus" icon={Banknote} /><MetricCard label="Active Delivery" value="1" change="Arriving in 18 min" icon={MapPin} /><MetricCard label="Your Rating" value="4.8" change="Top 10% this month" icon={Star} /></div><div className="mt-6 grid gap-5 lg:grid-cols-[1fr_360px]"><div className="rounded-2xl border border-border/60 bg-card p-5"><div className="flex items-center justify-between"><h2 className="font-display font-semibold">New delivery requests</h2><span className="rounded-full bg-primary/10 px-2 py-1 text-xs font-semibold text-primary">3 new</span></div>{[['ORD-2026-014','The Food House','₹42','1.2 km'],['ORD-2026-015','Pizza Corner','₹38','2.4 km'],['ORD-2026-016','Spice Villa','₹55','3.1 km']].map(([id,restaurant,fee,distance]) => <div key={id} className="mt-4 flex flex-wrap items-center gap-3 rounded-xl border border-border p-4"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary/10 text-primary"><Package className="h-5 w-5" /></div><div className="flex-1"><p className="font-semibold">{restaurant}</p><p className="mt-1 text-xs text-muted-foreground">{id} • {distance} away</p></div><div className="text-right"><p className="font-semibold text-success">{fee}</p><Button size="sm" className="mt-2" onClick={() => toast.success(`Delivery ${id} accepted`)}>Accept</Button></div></div>)}</div><div className="rounded-2xl border border-border/60 bg-card p-5"><h2 className="font-display font-semibold">Active delivery</h2><div className="mt-4 h-44 rounded-2xl bg-[#f5eee8] p-4"><div className="flex h-full items-center justify-center"><div className="rounded-xl bg-white px-4 py-3 text-center shadow"><MapPin className="mx-auto h-6 w-6 text-primary" /><p className="mt-1 text-sm font-semibold">Live route</p><p className="text-xs text-muted-foreground">Park Street → Salt Lake</p></div></div></div><div className="mt-4 flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-primary text-white"><Phone className="h-4 w-4" /></div><div><p className="font-semibold">Customer: Arjun Mehta</p><p className="text-xs text-muted-foreground">Call customer if needed</p></div></div></div></div></PanelShell>; }
