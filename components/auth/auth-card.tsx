'use client';

import Link from 'next/link';
import { ArrowLeft, Eye, EyeOff, Mail, Phone } from 'lucide-react';
import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { toast } from 'sonner';

interface AuthCardProps { mode: 'login' | 'register' | 'forgot' | 'otp'; }
export function AuthCard({ mode }: AuthCardProps) {
  const [showPassword, setShowPassword] = useState(false);
  const [submitted, setSubmitted] = useState(false);
  const title = mode === 'login' ? 'Welcome back' : mode === 'register' ? 'Create your account' : mode === 'forgot' ? 'Reset your password' : 'Verify your phone';
  const description = mode === 'login' ? 'Sign in to continue ordering your favorites.' : mode === 'register' ? 'Join FoodGo and get your first delivery on us.' : mode === 'forgot' ? 'Enter your email and we will send reset instructions.' : 'We sent a 6-digit code to your phone number.';
  const submit = () => { setSubmitted(true); toast.success(mode === 'login' ? 'Signed in successfully' : mode === 'otp' ? 'Phone verified' : 'Request submitted'); };
  return <div className="min-h-[calc(100vh-4rem)] bg-gradient-to-br from-secondary/60 via-background to-background px-4 py-10"><div className="mx-auto max-w-md"><Link href="/" className="flex items-center gap-2 text-sm text-muted-foreground hover:text-primary"><ArrowLeft className="h-4 w-4" /> Back to FoodGo</Link><div className="mt-5 rounded-3xl border border-border/60 bg-card p-6 shadow-xl sm:p-8"><div className="flex items-center gap-2"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary text-white"><span className="font-display text-xl font-bold">F</span></div><span className="font-display text-2xl font-bold">Food<span className="text-primary">Go</span></span></div><h1 className="mt-8 font-display text-3xl font-bold">{title}</h1><p className="mt-2 text-muted-foreground">{description}</p>{submitted ? <div className="mt-8 rounded-2xl bg-success/10 p-5 text-center text-success"><p className="font-semibold">You&apos;re all set.</p><p className="mt-1 text-sm">Continue to explore delicious food near you.</p><Link href="/" className="mt-4 inline-block"><Button>Start ordering</Button></Link></div> : <div className="mt-7 space-y-4">{mode === 'register' && <Input placeholder="Full name" className="h-11" />}{mode !== 'otp' && <><div className="relative"><Mail className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><Input placeholder="Email address" type="email" className="h-11 pl-10" /></div>{mode === 'register' && <div className="relative"><Phone className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" /><Input placeholder="Phone number" type="tel" className="h-11 pl-10" /></div>}{mode !== 'forgot' && <div className="relative"><Input placeholder="Password" type={showPassword ? 'text' : 'password'} className="h-11 pr-10" /><button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground">{showPassword ? <EyeOff className="h-4 w-4" /> : <Eye className="h-4 w-4" />}</button></div>}</>}{mode === 'otp' && <Input placeholder="Enter 6-digit OTP" inputMode="numeric" maxLength={6} className="h-12 text-center text-lg tracking-[0.4em]" />}<Button className="h-11 w-full" onClick={submit}>{mode === 'login' ? 'Sign in' : mode === 'register' ? 'Create account' : mode === 'forgot' ? 'Send reset link' : 'Verify OTP'}</Button>{mode === 'login' && <Link href="/forgot-password" className="block text-center text-sm text-primary hover:underline">Forgot password?</Link>}{mode === 'login' ? <p className="text-center text-sm text-muted-foreground">New to FoodGo? <Link href="/register" className="font-semibold text-primary">Create an account</Link></p> : mode === 'register' ? <p className="text-center text-sm text-muted-foreground">Already have an account? <Link href="/login" className="font-semibold text-primary">Sign in</Link></p> : <p className="text-center text-sm text-muted-foreground"><Link href="/login" className="font-semibold text-primary">Back to sign in</Link></p>}</div>}</div><p className="mt-5 text-center text-xs text-muted-foreground">By continuing, you agree to FoodGo&apos;s Terms and Privacy Policy.</p></div></div>;
}
